# inception
A small infrastructure project setting up a Dockerized environment with NGINX, WordPress, and MariaDB, each in its own container, orchestrated with Docker Compose inside a virtual machine.
